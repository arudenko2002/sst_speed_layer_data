{

    "sqlExpField" : {
        "Source-Staging":  "cast(master_track_id as string)  $INCLAUSE ($PARAMS)",
        "Source-Metadata":  "cast($ALIAS_PRODUCT.master_track_id as string)  $INCLAUSE ($PARAMS)"
    }


}